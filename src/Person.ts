/**
 * Author and Contributors type.
 */
export default interface Person {
  /** The person's name. */
  name: string;
  /** The person's email. */
  email?: string;
  /** The person's url. */
  url?: string;
}
